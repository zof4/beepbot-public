# Platform Root (Local Dev Mirror)

This directory mirrors the intended `/opt/platform` structure from the architecture doc.

Current local use:

- `opt/platform/monitoring/data/` for Uptime Kuma persistence.

As the platform evolves, place host-level configs and service manifests here.
